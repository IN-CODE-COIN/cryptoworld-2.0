services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-https://api.cryptoworld.cloud/api}
    ports:
      - "3600:80"
    restart: unless-stopped
    environment:
      - VITE_API_URL=https://api.cryptoworld.cloud/api
    networks:
      - cryptoworld-network

  backend:
    build: ./backend
    volumes:
      - ./backend:/var/www
    environment:
      # Aplicación
      APP_NAME: CryptoWorld
      APP_ENV: production
      APP_KEY: base64:3cgFE4bPHNf9ba45jDD6LI/vwHE5BeXEdWANQ9nkEfM=
      APP_DEBUG: false
      APP_URL: https://api.cryptoworld.cloud

      # Frontend
      FRONTEND_URL: https://cryptoworld.cloud

      # Base de datos
      DB_CONNECTION: mysql
      DB_HOST: cryptoworld-db-uc2ae5
      DB_PORT: 3306
      DB_DATABASE: cryptoworld
      DB_USERNAME: laravel
      DB_PASSWORD: bfvhe9iiyj7jjzla

      # Session
      SESSION_DRIVER: cookie
      SESSION_DOMAIN: .cryptoworld.cloud
      SESSION_SECURE_COOKIE: true
      SESSION_LIFETIME: 120

      # Sanctum
      SANCTUM_STATEFUL_DOMAINS: cryptoworld.cloud,www.cryptoworld.cloud

      # Redis
      REDIS_HOST: dokploy-redis.1.cp1qtw9zq2gg4rs74ezip8a0o
      REDIS_PASSWORD: null
      REDIS_PORT: 6379

      # API Keys
      COINRANKING_API_KEY: coinranking22b1d5f1ed17ba55bcd4a9c076747c62d4bca3520a85df31
    ports:
      - "9002:9000"

networks:
  cryptoworld-network:
    driver: bridge
