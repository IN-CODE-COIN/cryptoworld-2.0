services:
  frontend:
    build: ./frontend
    container_name: cryptoworld-frontend
    ports:
      - "3000:80"

  backend:
    build: ./backend
    container_name: cryptoworld-backend
    volumes:
      - ./backend:/var/www
    environment:
      # Variables para Laravel
      APP_NAME: CryptoWorld
      APP_ENV: production
      APP_KEY: base64:3cgFE4bPHNf9ba45jDD6LI/vwHE5BeXEdWANQ9nkEfM=
      APP_DEBUG: false
      APP_URL: https://midominio.com

      # Base de datos
      DB_CONNECTION: mysql
      DB_HOST: cryptoworld-db-uc2ae5
      DB_PORT: 3306
      DB_DATABASE: cryptoworld
      DB_USERNAME: laravel
      DB_PASSWORD: bfvhe9iiyj7jjzla

      # Redis (si usaras)
      REDIS_HOST: dokploy-redis.1.cp1qtw9zq2gg4rs74ezip8a0o
      REDIS_PASSWORD: null
      REDIS_PORT: 6379
    depends_on:
      - db
    ports:
      - "9000:9000"
